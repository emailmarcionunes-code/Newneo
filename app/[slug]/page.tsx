import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import {corePages,type CoreSlug} from '../../lib/core-pages';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const page=corePages[slug as CoreSlug];if(!page)return {};return{title:`${page.eyebrow} | Newneo`,description:page.lead,alternates:{canonical:`/${slug}/`}}}
export default async function CorePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=corePages[slug as CoreSlug];if(!page)notFound();return <main><header className="header"><nav className="nav wrap"><a className="brand" href="/">NEWNEO</a><div className="links"><a href="/ai-infrastructure">AI Infrastructure</a><a href="/ai-compute">AI Compute</a><a href="/agent-factory">Agent Factory</a><a href="/agentops">AgentOps</a><a className="navCta" href="/contact">Talk to an AI Architect →</a></div><a className="platformLogin" href="https://app.newneo.ai/login">Login <span aria-hidden="true">↗</span></a></nav></header><section className="hero"><div className="wrap heroGrid"><div><span className="eyebrow">{page.eyebrow}</span><h1>{page.title}</h1><p className="lead">{page.lead}</p><p className="productScope">Implementation is scoped to your environment. Connections, execution permissions and production readiness are validated during the pilot before live operation.</p><div className="actions"><a className="primary" href="#start">See how it works →</a><a className="secondary" href="/contact">Talk to an AI Architect</a></div></div><div className="stack">{page.items.map(([t,s],i)=><div className="stackCard" key={t}><small>0{i+1}</small><b>{t}</b><span>{s}</span></div>)}</div></div></section><section className="light" id="start"><div className="wrap"><div className="sectionHead"><div><span className="eyebrow darkEye">START HERE</span><h2>What do you need to accomplish?</h2></div><p>Start with the outcome. Newneo connects only the layers required to move from the current state to a production system.</p></div><div className="grid4">{page.items.map(([t,s],i)=><article className="card" key={t}><small>0{i+1}</small><h3>{t}</h3><p>{s}</p></article>)}</div></div></section><section className="dark"><div className="wrap split"><div><span className="eyebrow">PRODUCTION FIRST</span><h2>Designed as part of one enterprise AI system.</h2><p>Infrastructure, compute, agents and operations are separate layers, but they are engineered to work together when the business case requires it.</p></div><div className="questions"><div><b>Business before technology</b><span>Start from the process and measurable outcome.</span></div><div><b>Model-agnostic</b><span>Choose models and platforms according to the workload.</span></div><div><b>Operational by design</b><span>Security, observability and lifecycle controls are part of the architecture.</span></div></div></div></section><section className="cta"><div className="wrap ctaBox"><div><span className="eyebrow">LET'S START WITH THE PROBLEM</span><h2>What should this layer enable for your business?</h2><p>Bring us the workload, process or production challenge. We will help define the smallest useful next step.</p></div><a className="primary" href="/contact">Talk to an AI Architect →</a></div></section><footer className="footer wrap"><a className="brand" href="/">NEWNEO</a><span>Enterprise AI engineering.</span></footer></main>}
export function generateStaticParams(){return Object.keys(corePages).map(slug=>({slug}));}