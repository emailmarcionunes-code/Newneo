import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import {industryPages,type IndustrySlug} from '../../../lib/industry-pages';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const page=industryPages[slug as IndustrySlug];if(!page)return {};return{title:`${page.eyebrow} | Newneo`,description:page.lead,alternates:{canonical:`/industries/${slug}/`}}}
export default async function IndustryPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=industryPages[slug as IndustrySlug];if(!page)notFound();return <main><header className="header"><nav className="nav wrap"><a className="brand" href="/">NEWNEO</a><div className="links"><a href="/agent-factory">Agent Factory</a><a href="/rag">RAG</a><a href="/ai-security">AI Security</a><a href="/agentops">AgentOps</a><a className="navCta" href="/contact">Talk to an AI Architect →</a></div><a className="platformLogin" href="https://app.newneo.ai/login">Login <span aria-hidden="true">↗</span></a></nav></header><section className="hero"><div className="wrap heroGrid"><div><span className="eyebrow">{page.eyebrow}</span><h1>{page.title}</h1><p className="lead">{page.lead}</p><p className="productScope">Implementation is scoped to your environment. Connections, execution permissions and production readiness are validated during the pilot before live operation.</p><div className="actions"><a className="primary" href="#start">See where AI fits →</a><a className="secondary" href="/contact">Talk to an AI Architect</a></div></div><div className="stack">{page.items.map(([t,s],i)=><div className="stackCard" key={t}><small>0{i+1}</small><b>{t}</b><span>{s}</span></div>)}</div></div></section><section className="light" id="start"><div className="wrap"><div className="sectionHead"><div><span className="eyebrow darkEye">INDUSTRY PRIORITIES</span><h2>Start with the operating reality.</h2></div><p>Different industries have different data boundaries, workflows, risk models and infrastructure constraints. We design around those realities.</p></div><div className="grid4">{page.items.map(([t,s],i)=><article className="card" key={t}><small>0{i+1}</small><h3>{t}</h3><p>{s}</p></article>)}</div></div></section><section className="dark"><div className="wrap split"><div><span className="eyebrow">HOW NEWNEO APPROACHES IT</span><h2>AI that fits the operating model.</h2><p>We define the workflow, authority, data boundaries and measurable outcome before deciding what the AI should know, recommend or execute.</p></div><div className="questions"><div><b>Process fit</b><span>Start from the workflow and its operational owner.</span></div><div><b>Control fit</b><span>Respect privacy, policy, approvals and human authority.</span></div><div><b>Architecture fit</b><span>Choose infrastructure, models and tools according to the workload.</span></div></div></div></section><section className="cta"><div className="wrap ctaBox"><div><span className="eyebrow">START WITH ONE PRIORITY</span><h2>Where could AI create measurable value in your industry?</h2><p>We can map the process, define the boundaries and design a focused assessment or POC.</p></div><a className="primary" href="/contact">Talk to an AI Architect →</a></div></section><footer className="footer wrap"><a className="brand" href="/">NEWNEO</a><span>Enterprise AI engineering.</span></footer></main>}
export function generateStaticParams(){return Object.keys(industryPages).map(slug=>({slug}));}