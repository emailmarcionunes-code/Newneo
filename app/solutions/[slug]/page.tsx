import type {Metadata} from 'next';
import {notFound} from 'next/navigation';
import {solutionPages,type SolutionSlug} from '../../../lib/solution-pages';
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const page=solutionPages[slug as SolutionSlug];if(!page)return {};return{title:`${page.eyebrow} | Newneo`,description:page.lead,alternates:{canonical:`/solutions/${slug}`}}}
export default async function SolutionPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const page=solutionPages[slug as SolutionSlug];if(!page)notFound();return <main><header className="header"><nav className="nav wrap"><a className="brand" href="/"><i/>NEWNEO</a><div className="links"><a href="/agent-factory">Agent Factory</a><a href="/rag">RAG</a><a href="/mcp">MCP</a><a href="/agentops">AgentOps</a><a className="navCta" href="/contact">Talk to an AI Architect →</a></div></nav></header><section className="hero"><div className="wrap heroGrid"><div><span className="eyebrow">{page.eyebrow}</span><h1>{page.title}</h1><p className="lead">{page.lead}</p><div className="actions"><a className="primary" href="#start">See what AI can do →</a><a className="secondary" href="/contact">Talk to an AI Architect</a></div></div><div className="stack">{page.items.map(([t,s],i)=><div className="stackCard" key={t}><small>0{i+1}</small><b>{t}</b><span>{s}</span></div>)}</div></div></section><section className="light" id="start"><div className="wrap"><div className="sectionHead"><div><span className="eyebrow darkEye">BUSINESS OUTCOMES</span><h2>Start with one process.</h2></div><p>Choose the workflow where delay, repetition or inconsistency creates measurable friction. Then define exactly what AI should answer, recommend, execute or escalate.</p></div><div className="grid4">{page.items.map(([t,s],i)=><article className="card" key={t}><small>0{i+1}</small><h3>{t}</h3><p>{s}</p></article>)}</div></div></section><section className="dark"><div className="wrap split"><div><span className="eyebrow">HOW NEWNEO APPROACHES IT</span><h2>The business task is the unit of design.</h2><p>We map the workflow, systems, knowledge, approvals and measurable outcome first. The AI architecture follows that process.</p></div><div className="questions"><div><b>Map the workflow</b><span>Inputs, systems, ownership, handoffs and failure points.</span></div><div><b>Define AI authority</b><span>What can be read, recommended, executed or approved.</span></div><div><b>Measure the outcome</b><span>Task success, cycle time, intervention, quality and cost.</span></div></div></div></section><section className="cta"><div className="wrap ctaBox"><div><span className="eyebrow">START WITH ONE PROCESS</span><h2>Which workflow should AI improve first?</h2><p>We can map it, prove it with a focused POC and turn it into a governed production system.</p></div><a className="primary" href="/contact">Talk to an AI Architect →</a></div></section><footer className="footer wrap"><a className="brand" href="/"><i/>NEWNEO</a><span>Enterprise AI engineering.</span></footer></main>}
export function generateStaticParams(){return Object.keys(solutionPages).map(slug=>({slug}));}